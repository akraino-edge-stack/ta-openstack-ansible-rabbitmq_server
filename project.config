[access]
	inheritFrom = ta
[access "refs/*"]
	owner = group ldap/akraino-gerrit-ta-openstack-ansible-rabbitmq_server-committers
[project]
	description = This projects is re-used from the upstream Openstack-Ansible project (https://github.com/openstack/openstack-ansible ) for the purpose of deploying Rabbit MQ. These services are used by various middleware, and deployer components.
